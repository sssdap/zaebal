# 🚀 Руководство по развёртыванию ILLUMINOS

Это руководство поможет вам развернуть проект на различных платформах.

## 📋 Подготовка к развёртыванию

### 1. Сборка проекта

```bash
npm run build
```

Эта команда создаст оптимизированную версию приложения в папке `build/`.

### 2. Тестирование сборки локально

```bash
# Установите serve глобально
npm install -g serve

# Запустите сборку
serve -s build
```

Приложение будет доступно по адресу `http://localhost:3000`

## 🌐 Развёртывание на различных платформах

### Vercel (Рекомендуется)

1. **Установите Vercel CLI**
```bash
npm install -g vercel
```

2. **Разверните проект**
```bash
vercel
```

3. **Для продакшена**
```bash
vercel --prod
```

**Или через веб-интерфейс:**
1. Зайдите на [vercel.com](https://vercel.com)
2. Импортируйте репозиторий из GitHub
3. Vercel автоматически определит настройки React
4. Нажмите "Deploy"

### Netlify

1. **Через Netlify CLI**
```bash
# Установите Netlify CLI
npm install -g netlify-cli

# Разверните
netlify deploy --prod
```

2. **Через веб-интерфейс:**
   - Зайдите на [netlify.com](https://netlify.com)
   - Подключите GitHub репозиторий
   - Build command: `npm run build`
   - Publish directory: `build`
   - Нажмите "Deploy site"

### GitHub Pages

1. **Установите gh-pages**
```bash
npm install --save-dev gh-pages
```

2. **Добавьте в package.json**
```json
{
  "homepage": "https://yourusername.github.io/illuminos",
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
}
```

3. **Разверните**
```bash
npm run deploy
```

### Heroku

1. **Создайте Procfile**
```
web: serve -s build
```

2. **Добавьте serve в dependencies**
```bash
npm install serve --save
```

3. **Разверните**
```bash
heroku create illuminos-app
git push heroku main
```

### Firebase Hosting

1. **Установите Firebase CLI**
```bash
npm install -g firebase-tools
```

2. **Инициализируйте Firebase**
```bash
firebase init hosting
```

3. **Настройте firebase.json**
```json
{
  "hosting": {
    "public": "build",
    "ignore": ["firebase.json", "**/.*", "**/node_modules/**"],
    "rewrites": [
      {
        "source": "**",
        "destination": "/index.html"
      }
    ]
  }
}
```

4. **Разверните**
```bash
npm run build
firebase deploy
```

## ⚙️ Переменные окружения

Для продакшена создайте файл `.env.production`:

```env
REACT_APP_API_URL=https://api.illuminos.com
REACT_APP_PAYMENT_KEY=your_production_key
```

## 🔒 Безопасность

### Перед развёртыванием:
- ✅ Удалите все console.log из кода
- ✅ Проверьте, что API ключи не захардкожены
- ✅ Настройте CORS на backend
- ✅ Включите HTTPS
- ✅ Настройте CSP заголовки

## 📊 Мониторинг

### Рекомендуемые инструменты:
- **Google Analytics** - аналитика посещений
- **Sentry** - отслеживание ошибок
- **Lighthouse** - проверка производительности

## 🔄 CI/CD

### GitHub Actions пример

Создайте `.github/workflows/deploy.yml`:

```yaml
name: Deploy to Vercel

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install dependencies
        run: npm install
      - name: Build
        run: npm run build
      - name: Deploy to Vercel
        uses: amondnet/vercel-action@v20
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.ORG_ID }}
          vercel-project-id: ${{ secrets.PROJECT_ID }}
```

## 📝 Чеклист развёртывания

- [ ] Код собирается без ошибок
- [ ] Все тесты проходят
- [ ] Проверена адаптивность
- [ ] Оптимизированы изображения
- [ ] Настроены переменные окружения
- [ ] Настроен домен (если нужно)
- [ ] Настроен SSL сертификат
- [ ] Проверена производительность (Lighthouse)
- [ ] Настроен мониторинг ошибок

## 🆘 Решение проблем

### Белый экран после развёртывания
- Проверьте консоль браузера на ошибки
- Убедитесь, что `homepage` в package.json настроен правильно
- Проверьте пути к ресурсам

### 404 при обновлении страницы
- Настройте rewrites для SPA
- Для Netlify: создайте `public/_redirects`:
```
/*    /index.html   200
```

### Медленная загрузка
- Включите сжатие gzip
- Оптимизируйте изображения
- Используйте code splitting
- Настройте кэширование

---

Удачного развёртывания! 🚀
